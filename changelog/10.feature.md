Added a `release-changelog-path` input to the reusable `bump.yaml` workflow,
forwarded to the `draft-release` action as `changelog-path`.
This lets consumers point the release-notes extraction at a non-default
changelog location (e.g. `docs/changelog.md` for towncrier projects) without a
custom `release-changelog-script`.
